# SPEACE – Python Dependencies
# Installa con: pip install -r requirements.txt

# Core
pyyaml>=6.0
requests>=2.31.0
psutil>=5.9.0

# AI / LLM (per Team Scientifico con Claude API)
anthropic>=0.25.0

# Scheduling (per evolver/monitor in background)
schedule>=1.2.0

# Utility
python-dotenv>=1.0.0

# Testing (M1 – PROP-CORTEX-COMPLETE-M1)
pytest>=8.0.0
